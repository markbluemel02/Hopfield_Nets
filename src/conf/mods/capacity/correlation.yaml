# @package _global_
capacity: #for getting accurate results on capacity
  interrupt: True
  threshold: 0.95 #when training will STOP
  epsilon: 0.95 #when training will count as SUCCESSFUL
  n_checkpoints: 15
  start: 1.5
  step: 0.1
  stop: 5

params:
  exp_name : 'Capacity'

storage:
  capacity: True