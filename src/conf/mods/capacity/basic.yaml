# @package _global_
capacity:
  interrupt: False
  threshold: 1 #when training will STOP, basically unusable
  epsilon: 0.95 #when training will count as SUCCESSFUL
  start: 0.2
  step: 0.2
  stop: 1.21

params:
  exp_name : 'Capacity'

storage:
  capacity: True