# @package _global_
capacity: 
  interrupt: False
  threshold: 1 #when training will STOP
  epsilon: 0.95 #when training will count as SUCCESSFUL
  n_checkpoints: 15
  start: 0.01
  step: 0.05
  stop: 2.02

params:
  exp_name : 'Capacity'

storage:
  capacity: True