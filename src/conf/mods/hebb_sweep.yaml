# @package _global_
defaults:
 - override /model: hebbian_start
 - override /binning: adaptive

hebb_sweep:
  start: 0.01
  stop: 0.3
  n_points: 30
  mode: 'lin' #'lin' possible, 'log' tba

params:
  exp_name : 'HebbSweep'